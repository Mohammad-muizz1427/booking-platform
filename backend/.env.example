PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# Supabase → Project Settings → Database → Connection string (URI)
# Use the "Transaction" pooler mode for server-side apps, or direct connection for migrations.
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.xrwdsvsivdbehgmecfqy.supabase.co:5432/postgres

# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=7d
